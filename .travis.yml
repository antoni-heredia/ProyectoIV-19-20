language: python
python:
  - "3.5"
  - "3.6"    
  - "3.7"
# command to install dependencies
install:
  - sudo apt-get install rabbitmq-server
  - make dependencias
# command to run tests
script:
  - make tests